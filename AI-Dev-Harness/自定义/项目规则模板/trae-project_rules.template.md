# 项目规则（Trae IDE / TraeWork / TraeCode CLI 用；开发期存在，完成后自动删除）

本项目使用上级目录的通用配置 `..\AI-Dev-Harness\`。
- 请先完整读取 `..\AI-Dev-Harness\自定义\对话协议.md` 并严格遵循。
- 工作文件统一放在 `.harness\` 下，不要污染项目目录。
- 严格按 `方案.md` 与 `需求与理念.md` 执行，不擅自扩大或缩小范围。
- 全程中文对话；用户不输入命令；所有脚本按需运行，收尾关闭本次开启的所有进程。
- 完成并验收后，按对话协议执行纯净化清理。
