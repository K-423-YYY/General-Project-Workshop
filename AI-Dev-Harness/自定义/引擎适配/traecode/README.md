# TraeCode CLI / Trae 系列适配（**未查明**）

> 第 6 批探测结论：**未查明**。本文如实记录「查到什么、没查到什么、下一步怎么查」，
> **不写任何推测性结论**。

> **第 3 段状态（2026-09-12）**：本机仍无 `traecode` 命令（统一解析器
> `../../scripts/lib/engine-detect.mjs` 探过 PATH 与已知安装位置；能力表把它标为
> 「本轮跳过实测」，并保留 2026-09-11 的 `lastVerified`）。
> 但 **Trae IDE 有图形端在机**（`trae.cmd` / `%APPDATA%\Trae`）—— 它作为
> 独立引擎项 `trae-ide` 进了 `../../scripts/lib/engine-detect.mjs` 的引擎目录，
> 在 `run.bat` 的列表里可选（选中后只生成启动词 + 提示在 IDE 里打开项目目录）。
> 完整对照见 [`../引擎能力矩阵.md`](../引擎能力矩阵.md)。

---

## 一、本批实际确认到的事实

| 事实 | 怎么确认的 |
|---|---|
| `traecode` **不在 PATH** | `where traecode` 无结果（探测脚本每次都会重新确认） |
| Trae IDE 的配置目录 `~/.trae` **存在** | 目录侦察（只看有没有，不读内容） |
| 规则文件位置 `.trae/rules/` | 来自既有适配器文档 `内置/adapters/traecode-cli.md`，**本批未验证** |

## 二、因此哪些结论**不能**下

| 想回答的问题 | 本批结论 |
|---|---|
| TraeCode CLI 有没有 hooks / 插件机制？ | **未查明** |
| 它的机制怎么配置？键名是什么？ | **未查明** |
| 它能被拦 shell 命令吗？ | **未查明**（能确定的只有：走 `run.bat` 启动时 U4 命令包装对它生效） |
| Trae IDE 能注入机制层吗？ | **不能**——它是图形界面，不走 `run.bat`，没有可编程入口 |

> 注意 **`未查明` ≠ `不支持`**。能力表里这一列也是这么标的。

## 三、下一步怎么查（给第 7 批或以后）

```powershell
# 1. 先装 CLI（需要用户本人决定）
# 2. 然后全部只读地探：
traecode --help
traecode --version
traecode features        # 若有
traecode <子命令> --help

# 3. 探配置格式仍然遵守三条铁律：
#    · 绝不读 ~/.trae 下可能含凭据的文件
#    · 只用只读命令
#    · 靠报错信息推断合法键名
```

装上之后，`node ../../scripts/probe-engines.mjs --engine traecode-cli` 会自动把结论补进能力表
（脚本已经为它留好了分支，只是当前 `installed=false` 所以走「未安装，未探测」那条路）。

## 四、Trae 系列当前的兜底覆盖

即使机制层未查明，Trae 也不是完全没有保障：

| 层 | 对 Trae 是否生效 | 说明 |
|---|---|---|
| **U1 规则单一来源** | ✅ | `.trae/rules/` 读规则文件（由 `gen-rules.mjs` 在第 7 批生成） |
| **U2 启动词注入** | ✅ | 只要走 `run.bat`，启动词就带 40 行硬规则 |
| **U3 会话后审计** | ✅ | 审计读的是命令记录与 git diff，与引擎无关 |
| **U4 命令包装** | ⚠️ 仅走 harness 时 | 在 Trae IDE 里直接开对话 → 不走 `run.bat` → 不生效 |
| **U5 机制层** | ❌ 未查明 | 本批结论 |
