# 规则源（唯一真源）

<!-- harness:rules-source
version: 1
updated: 2026-09-11
generator: AI-Dev-Harness\自定义\scripts\gen-rules.mjs
-->

> **本文件是全部「硬规则」文本的唯一真源。**
> 下面 4 份规则文件 + `对话协议.md` 的第 10 章**全部由本文件生成**，不要手改生成物——
> 改了会在下次生成时被覆盖（`gen-rules.mjs --check` 会报出漂移）。
>
> | 生成物 | 谁在用 | 引擎标签 |
> |---|---|---|
> | `自定义/项目规则模板/CLAUDE.template.md` | Claude Code 项目（`scaffold.ps1` / `scaffold-ext.mjs` 复制进项目） | `@claude` |
> | `自定义/项目规则模板/AGENTS.template.md` | Codex / DSH / 根目录入口 | `@codex` |
> | `自定义/项目规则模板/trae-project_rules.template.md` | Trae IDE / TraeWork / TraeCode CLI | `@trae` |
> | `自定义/prompt-模板/硬规则.md` | U2 启动词注入（`build-prompt.mjs`） | `@default` |
> | `自定义/对话协议.md` 第 10 章（标记区） | 协议本体 | `@default` |
>
> 命令：
> ```
> node AI-Dev-Harness\自定义\scripts\gen-rules.mjs            # 生成/更新全部生成物
> node AI-Dev-Harness\自定义\scripts\gen-rules.mjs --check     # 只检查是否同源（不改文件）
> node AI-Dev-Harness\自定义\scripts\gen-rules.mjs --dry-run   # 预演
> ```
>
> **改规则的正确姿势**：只改本文件里的 `硬规则` 块（或 `开工第一步` 块），然后跑一次 `gen-rules.mjs`。
> 改完务必跑 `--check` 确认 5 个消费方全部同源。

---

## 语法（给维护者看，生成器按这个解析）

下面用 `#vars` / `#block` / `#out` 指代三种定义，实际书写时它们各自包在 HTML 注释里
（形如 `<!--` + 空格 + `#vars` + 空格 + 参数 + 空格 + `-->`），并以一行 `#end` 注释收尾。
**本节只是说明，不写真实注释**——写了会被解析器当成真定义。

| 写法 | 含义 |
|---|---|
| `#vars @引擎标签,另一个标签` | 该标签下 `{{var:名}}` 的取值；每行一个 `名 = 值` |
| `#block 块名 @引擎标签` | 可被 `{{block:块名}}` 引用；带 `@` 的是引擎专属变体，不带 `@` 是默认变体 |
| `#out 相对路径 @引擎标签` | 生成物；正文是完整文件内容（相对 `AI-Dev-Harness\`） |
| `#out 相对路径 @引擎标签 region=标记名` | 就地同步到既有文件的标记区内 |

- `{{block:块名}}` 在「有该引擎专属变体」时优先取变体，否则取默认变体。
- `region=标记名` 会在目标文件里找 `<!-- 标记名:begin -->` / `<!-- 标记名:end -->`，
  只替换这两个标记之间的内容；标记不存在就报错（不猜、不追加）。
- 解析是**扁平**的：定义不可嵌套，`#end` 关闭最近打开的那个定义。

---

<!-- #vars @default -->
改码工具 = Edit / Write 工具
子代理 = 子代理（Explore）
<!-- #end -->

<!-- #vars @trae -->
改码工具 = 编辑 / 写入工具
子代理 = 子代理
<!-- #end -->

<!-- #block 硬规则正文 -->
**一、改码方式**
改代码只用 {{var:改码工具}}。禁止 `sed -i`、`perl -i`、`cat > 源文件`、`python`/`perl` 内联改码
—— 内联替换在 Windows 下会**静默失配**（退出码 0 但文件没改），改坏了你还不知道。
不执行破坏性命令（`rm -rf`、`git reset --hard`、`git push --force`），除非用户明确要求。
不读取、不打印任何密钥 / API key / `.env` 内容。

**二、测试分层（禁止「改一行 → 跑全量」）**
L1 改动后 → 只跑**定向单测**（对应测试文件，秒级）。
L2 任务边界 → 全量单测 + 类型检查（+ lint）。
L3 阶段收尾 → 才跑全量 E2E；配额 2 次 / 会话，**失败不计数**。
每完成一个小功能做最小验证，通过再进下一步。
失败先看 stderr / 日志再改，禁止「猜 → 改 → 重跑」。
验收不达标 → 如实记录，禁止造假数据、禁止把失败说成通过。

**三、上下文与成本（省 token 六策略）**
1. **分层加载**：只读当前任务相关文件，不通读整库。
2. **忽略大目录**：`node_modules`、`dist`、`.git`、构建产物一律不读、不 grep。
3. **记忆外置**：已读摘要与关键决策写进 `.harness\`（JOURNAL / HANDOFF），靠文件记，不靠上下文记。
4. **只给 git diff**：汇报改动用 `git diff` / `git status --short`，不要重述整份文件。
5. **限制轮次**：单任务设轮次上限；连续 2 轮无进展立即停下汇报，不要盲目重试。
6. **只读错误输出**：长输出命令重定向到文件，只读回**失败项 + 末尾**，不要把整屏日志灌进上下文。
**拒绝全量代码库输入**；**严禁全量载入项目不相关代码历史**；探索类任务交给{{var:子代理}}。

**四、文件与目录**
只改动项目文件夹与 `AI-Dev-Harness\state\`（归档）；不要修改 `AI-Dev-Harness\内置\`。
状态记录用 **append 追加**，不要重写整篇。
严格按 `方案.md` 与 `需求与理念.md` 执行，不擅自扩大或缩小范围。

**五、运行纪律**
不使用后台常驻进程、定时任务、开机自启、文件监听；脚本按需运行，收尾关闭本次开启的所有进程。
所有路径用**相对路径**，不要写死绝对路径。
不让用户去开终端；一切终端 / 脚本操作由你自行完成。

**六、协作方式**
全程中文对话。方向性决策由用户决定；AI 只提建议、做执行。
卡住 → 立即停止并报告，不要绕圈。契约变更 → 先在 `STATUS.md` 写明提案，征得用户同意后再改。
<!-- #end -->

<!-- #block 硬规则 -->
## 硬规则（逐条必须遵守，违反即返工）

{{block:硬规则正文}}
<!-- #end -->

<!-- #block 铁律正文 -->
**一、项目开发期间只动两处**
只能改动：① 项目文件夹（`我的项目\`，阶段 3 起用项目名）；② `AI-Dev-Harness\state\`（归档与运行态）。
其余任何位置一律不动；需要动，先停下来问用户。

**二、机制层属框架资产，不得修改**
`.claude\`、`自定义\bin\`、`自定义\scripts\`、`自定义\规则源.md`、`自定义\模型能力表.json`、
`自定义\引擎能力表.json`、`自定义\引擎适配\`、`自定义\prompt-模板\`、`日志\`
—— 这些是 harness 的机制层（框架资产），项目开发期间**一律不得改动**（`日志\` 只追加，不改写、不提交）。

**三、内置区仍不可动（附例外口径）**
`AI-Dev-Harness\内置\`（`adapters\`、`engine\`、`scripts\`、`设计文档\`）仍是不可动区。
**要改内置区或机制层，先确认：「这是修 harness，不是做项目」** —— 用户明确确认后才动，
并在 `state\` 留一条改动记录（改了哪个文件、为什么、怎么回退）。

**四、交付前必须跑纯净度自检**
跑 `node AI-Dev-Harness\自定义\scripts\purity-check.mjs --project "<项目目录>"`，必须返回 0（结论「通过」）：
项目文件夹里不得残留 `.claude\`、`.harness\`、`.trae\`、`AGENTS.md`、`CLAUDE.md`，也不得有 harness 路径引用。
清理先出「将删除 / 将保留」清单、等用户确认（`cleanup.ps1` 不带 `-Confirm` 时一个文件都不删）。

**五、GitHub 只告知，不代推**
远端同步（建仓库、`git remote add`、`git push`、改可见性）默认**只输出步骤与命令**，由用户自己执行；
只有用户明确说「你帮我推 / 你代做」时才代做，且执行后要把原始输出给用户看。

**六、四条老条款继续有效**
1. 所有路径用**相对路径**，不写死绝对路径（工作区可整体复制到任何电脑、任何目录、任何用户名）。
2. **不使用后台常驻进程**、定时任务、开机自启、文件监听；脚本按需运行，收尾关闭本次开启的所有进程。
3. **全程中文**对话。
4. **用户只说话、不输命令**：一切终端与脚本操作由 AI 自行完成。

> **本铁律更新完即锁定**，之后不再改动；要改，先确认"这是修 harness，不是做项目"。
<!-- #end -->

<!-- #block 铁律 -->
## 铁律（框架边界，逐条必须遵守）

{{block:铁律正文}}
<!-- #end -->

<!-- #block 开工第一步 -->
## 开工第一步
先跑体检：`node ..\AI-Dev-Harness\自定义\scripts\env-doctor.mjs`，再按「阶段 0 → 阶段 4」推进。
被机制层拦下（`.claude/` 的 hook 或命令包装）时，**照它给的替代方案做，不要换种写法绕过**；
确实需要例外时建 `.harness\bypass` 文件，用完删掉。
完成并验收后，按对话协议执行纯净化清理。
<!-- #end -->

<!-- #block 开工第一步 @trae -->
## 开工第一步
先跑体检（在项目目录内执行）：`node ..\AI-Dev-Harness\自定义\scripts\env-doctor.mjs`，再按「阶段 0 → 阶段 4」推进。
Trae 没有 Claude Code 那样的 hook 机制层，**本文件是唯一防线，请逐条照做**。
被命令行包装拦下时，照它给的替代方案做，不要换种写法绕过。
完成并验收后，按对话协议执行纯净化清理。
<!-- #end -->

<!-- #out 自定义/项目规则模板/CLAUDE.template.md @claude -->
# 项目规则（Claude Code 用；开发期存在，完成后自动删除）

<!-- 本文件由 AI-Dev-Harness\自定义\规则源.md 自动生成（gen-rules.mjs），不要手改。
     它必须「自包含」：断网、不打开任何其它文件，也能知道全部硬规则。
     要改规则 → 改 规则源.md 的「硬规则正文」块，再跑 gen-rules.mjs。 -->

本项目使用上级目录的通用配置 `..\AI-Dev-Harness\`。工作文件统一放 `.harness\`，不要污染项目目录。

{{block:硬规则}}

{{block:铁律}}

{{block:开工第一步}}
<!-- #end -->

<!-- #out 自定义/项目规则模板/AGENTS.template.md @codex -->
# 项目规则（由 AI-Dev-Harness 自动生成；开发期存在，完成后自动删除）

<!-- 本文件由 AI-Dev-Harness\自定义\规则源.md 自动生成（gen-rules.mjs），不要手改。
     它必须「自包含」：断网、不打开任何其它文件，也能知道全部硬规则。
     要改规则 → 改 规则源.md 的「硬规则正文」块，再跑 gen-rules.mjs。 -->

本项目使用上级目录的通用配置 `..\AI-Dev-Harness\`。工作文件统一放 `.harness\`，不要污染项目目录。

{{block:硬规则}}

{{block:铁律}}

{{block:开工第一步}}
<!-- #end -->

<!-- #out 自定义/项目规则模板/trae-project_rules.template.md @trae -->
# 项目规则（Trae IDE / TraeWork / TraeCode CLI 用；开发期存在，完成后自动删除）

<!-- 本文件由 AI-Dev-Harness\自定义\规则源.md 自动生成（gen-rules.mjs），不要手改。
     它必须「自包含」：断网、不打开任何其它文件，也能知道全部硬规则。
     放置位置：复制到 `<项目>\.trae\rules\project_rules.md`。
     要改规则 → 改 规则源.md 的「硬规则正文」块，再跑 gen-rules.mjs。 -->

本项目使用上级目录的通用配置 `..\AI-Dev-Harness\`。工作文件统一放 `.harness\`，不要污染项目目录。

{{block:硬规则}}

{{block:铁律}}

{{block:开工第一步}}
<!-- #end -->

<!-- #out 自定义/prompt-模板/硬规则.md @default -->
{{block:硬规则}}

{{block:铁律}}
<!-- #end -->

<!-- #out 自定义/对话协议.md @default region=harness:rules -->
## 10. 硬规则（逐条必须遵守，违反即返工）

> 本章由 `自定义\规则源.md` 自动生成（`gen-rules.mjs --check` 可验证同源），**不要手改**。
> 第 11–13 章是本章的展开（上下文预算 / 验证分层 / 省 token），本章是规则的唯一正文。

{{block:硬规则正文}}

## 10.1 铁律（框架边界 · 已锁定）

> 同上：由 `自定义\规则源.md` 生成，`gen-rules.mjs --check` 可验证与其它四处逐字同源。

{{block:铁律正文}}
<!-- #end -->

<!-- #out ../AGENTS.md @default region=harness:ironrules -->
{{block:铁律正文}}
<!-- #end -->

<!-- #out 自定义/对话协议.md @default region=harness:ironrules -->
{{block:铁律正文}}
<!-- #end -->

<!-- #out ../项目说明/03-使用说明.md @default region=harness:ironrules -->
{{block:铁律正文}}
<!-- #end -->
