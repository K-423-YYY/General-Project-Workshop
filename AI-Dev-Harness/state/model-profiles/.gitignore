# 模型能力画像 —— 运行时产物，不进仓库
#
# 为什么不提交（02-改进方案/02-逐项改动清单.md A15）：
#   画像文件里含 API 端点地址（如 api.deepseek.com）和本机观察到的用量特征，
#   属于本机私有状态，不该随仓库分发。
#
# 生效范围：本目录（含子目录）。
# 需要提交的东西请显式加 ! 例外，例如下面的 README.md。

*
!.gitignore
!README.md
