# 通用 AI 项目开发工作区（入口规则）

本文件夹是一个**可整体复制的通用项目开发工作区**：把整个文件夹复制到任意位置、交给任意 AI（Codex / Claude Code / DeepSeek Harness / Trae），即可开始做项目。

## 一、本工作区结构
- `AI-Dev-Harness\` —— 通用开发配置（对话协议、技能、适配、脚本），所有说明都在里面。
- `我的项目\` —— 项目文件夹。开始一个项目后，它会被改名为项目名。
- `AGENTS.md` —— 本入口规则（Codex / Claude Code 打开本文件夹时自动读取）。

## 二、开工第一步（必须执行）
1. 用中文向用户问好，并问：你想做什么项目？（一句话即可）
2. **完整读取** `AI-Dev-Harness\自定义\对话协议.md`，严格按其中的「阶段 0 → 阶段 4」流程执行。
3. 全程用中文对话，不要让用户输入任何命令；一切脚本/终端操作由你（AI）在后台自行完成，并在收尾时关闭自己开启的所有进程。

## 三、铁律
- 所有路径一律使用**相对路径**，不要写死绝对路径（本工作区可复制到任何电脑、任何目录、任何用户名）。
- 不使用后台常驻进程、定时任务、开机自启、文件监听；所有脚本只在需要时**按需运行**。
- 只改动 `我的项目\`（项目文件夹）和 `AI-Dev-Harness\state\`（归档）；不要修改 `AI-Dev-Harness\内置\`。
- 用户可自由修改的是 `AI-Dev-Harness\自定义\`。

## 四、下一步
按 `AI-Dev-Harness\自定义\对话协议.md` 执行。若用户已直接说明要做的项目，先复述确认，再进入阶段 1。
