# 通用 AI 项目开发工作区（入口规则）

本文件夹是一个**可整体复制的通用项目开发工作区**：把整个文件夹复制到任意位置、交给任意 AI（Codex / Claude Code / DeepSeek Harness / Trae），即可开始做项目。

## 一、本工作区结构
- `AI-Dev-Harness\` —— 通用开发配置（规则、技能、脚本、适配），所有说明都在里面。
  - `自定义\对话协议.md` —— **总规则**（第 8 章可改区/内置区/机制层、第 10 章硬规则、第 14 章机制层边界是核心）。
  - `自定义\规则源.md` —— **硬规则与铁律的唯一真源**；改规则只改这里，再跑 `gen-rules.mjs` 同步 5 处消费方。
  - `自定义\scripts\env-doctor.mjs` —— **开工体检 + 启动词生成**（阶段 0 第一步就跑它）。
  - `自定义\scripts\purity-check.mjs` —— 交付前**纯净度自检**（项目里不得残留 harness 痕迹）。
  - `自定义\项目规则模板\` —— 建项目时复制进项目的规则文件（已内联硬规则与铁律，自包含）。
- `我的项目\` —— 项目文件夹。开始一个项目后，它会被改名为项目名。
- `.claude\` —— 机制层（hooks 与状态）。由 harness 维护，**项目开发期间不得修改**，但要知道它存在。
- `日志\` —— 日志中心（8 个分类，只追加、不进 git、不随项目交付）。
- `AGENTS.md` —— 本入口规则（Codex / Claude Code 打开本文件夹时自动读取）。

## 二、开工第一步（必须执行）
1. **先跑体检**（不要跳过，也不要等用户提醒）：
   ```
   node AI-Dev-Harness\自定义\scripts\env-doctor.mjs
   ```
   它报出：模型窗口陷阱（模型名带 `[1M]` → 压缩阈值被抬到 96.7 万）、hooks 是否注册、
   规则模板是否已升级、有无上次遗留的 HANDOFF。**它输出的末尾就是本会话的启动词。**
   （不要自动加 `--fix`；修复必须由用户显式要求。）
2. 用中文向用户问好，并问：你想做什么项目？（一句话即可）
   - **若用户手上已有方案 / 计划文档**：请他放进项目文件夹（`我的项目\`）并说明路径，走
     `自定义\对话协议.md` 第 2 章的「已有方案」分支（先只读核对 → 再归并成 `方案.md` / `需求与理念.md`），
     **不要重复从零设计方案**。
3. 读 `AI-Dev-Harness\自定义\对话协议.md`；**若本会话已由机制层注入硬规则，可只读其中的流程章节（第 1–9 章）**。
4. 全程用中文对话，不要让用户输入任何命令；一切脚本/终端操作由你（AI）在后台自行完成，并在收尾时关闭自己开启的所有进程。

## 三、硬规则（速查 · 完整版见对话协议第 10 章）
1. **改码只用 Edit / Write**。禁止 `sed -i`、`perl -i`、`cat > 源文件`、`python` 内联改码
   —— Windows 下会静默失配（退出码 0 但文件没改），改坏了你还不知道。被机制层拦下就照它给的替代方案做，**不要换种写法绕过**。
2. **测试分层**：改动 → 定向单测（秒级）；任务边界 → 全量单测 + 类型检查；阶段收尾 → 全量 E2E（**配额 2 次/会话，失败不计数**）。
   禁止「改一行 → 跑 playwright」。
3. **失败先看 stderr / 日志再改**，禁止「猜 → 改 → 重跑」。
4. **长输出命令重定向到文件，只读回摘要**；不要重述整屏日志。
5. **改前先读完整文件**；同一文件不要反复改（第 8 次起会收到警告）。
6. **探索类任务交给子代理（Explore）**，不要把整库读进主上下文。
7. **上下文用到 70% 就该结束会话并写 HANDOFF**，不要硬撑到压缩开始丢细节。
8. **独立验收跑 `.claude\hooks\verify-gate.mjs`**，引用脚本原始输出，不要自述「已全部通过」。

## 四、省 token 六策略（完整版见对话协议第 13 章）
分层加载 · 忽略大目录（`node_modules`/`dist`/`.git`）· 记忆外置到 `.harness\` · 只给 `git diff` ·
限制轮次（连续 2 轮无进展立即停）· 只读错误输出。
**拒绝全量代码库输入**；**严禁全量载入项目不相关代码历史**；换功能先开新会话。

## 五、铁律

> 本节由 `AI-Dev-Harness\自定义\规则源.md` 自动生成（`gen-rules.mjs --check` 可验证 5 处逐字同源），**不要手改**。
> 需要临时放行机制层拦截时，建 `.harness\bypass` 文件，**用完立即删除**。

<!-- harness:ironrules:begin -->

**一、项目开发期间只动两处**
只能改动：① 项目文件夹（`我的项目\`，阶段 3 起用项目名）；② `AI-Dev-Harness\state\`（归档与运行态）。
其余任何位置一律不动；需要动，先停下来问用户。

**二、机制层属框架资产，不得修改**
`.claude\`、`自定义\bin\`、`自定义\scripts\`、`自定义\规则源.md`、`自定义\模型能力表.json`、
`自定义\引擎能力表.json`、`自定义\引擎适配\`、`自定义\prompt-模板\`、`日志\`
—— 这些是 harness 的机制层（框架资产），项目开发期间**一律不得改动**（`日志\` 只追加，不改写、不提交）。

**三、内置区仍不可动（附例外口径）**
`AI-Dev-Harness\内置\`（`adapters\`、`engine\`、`scripts\`、`设计文档\`）仍是不可动区。
**要改内置区或机制层，先确认：「这是修 harness，不是做项目」** —— 用户明确确认后才动，
并在 `state\` 留一条改动记录（改了哪个文件、为什么、怎么回退）。

**四、交付前必须跑纯净度自检**
跑 `node AI-Dev-Harness\自定义\scripts\purity-check.mjs --project "<项目目录>"`，必须返回 0（结论「通过」）：
项目文件夹里不得残留 `.claude\`、`.harness\`、`.trae\`、`AGENTS.md`、`CLAUDE.md`，也不得有 harness 路径引用。
清理先出「将删除 / 将保留」清单、等用户确认（`cleanup.ps1` 不带 `-Confirm` 时一个文件都不删）。

**五、GitHub 只告知，不代推**
远端同步（建仓库、`git remote add`、`git push`、改可见性）默认**只输出步骤与命令**，由用户自己执行；
只有用户明确说「你帮我推 / 你代做」时才代做，且执行后要把原始输出给用户看。

**六、四条老条款继续有效**
1. 所有路径用**相对路径**，不写死绝对路径（工作区可整体复制到任何电脑、任何目录、任何用户名）。
2. **不使用后台常驻进程**、定时任务、开机自启、文件监听；脚本按需运行，收尾关闭本次开启的所有进程。
3. **全程中文**对话。
4. **用户只说话、不输命令**：一切终端与脚本操作由 AI 自行完成。

> **本铁律更新完即锁定**，之后不再改动；要改，先确认"这是修 harness，不是做项目"。
<!-- harness:ironrules:end -->

## 六、下一步
按 `AI-Dev-Harness\自定义\对话协议.md` 执行。若用户已直接说明要做的项目，先复述确认，再进入阶段 1。
