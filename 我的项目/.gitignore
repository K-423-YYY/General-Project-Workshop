# 你的项目就放在这个文件夹里（阶段 3 会把本文件夹改名为项目名）。
#
# 本文件同时干两件事：
#   1. 让「我的项目\」这个目录本身随仓库走 —— git 不跟踪空目录，没有它就克隆不出这个文件夹；
#   2. 保护你的项目内容：你放进来的代码/文档不会被误提交进 harness 仓库
#      （项目自己的版本控制请用项目内的 git；规则写在该项目自己的 .gitignore 里）。
*
!.gitignore
