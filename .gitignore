# ============================================================
# 通用项目工作区 — 仓库级忽略规则
#
# 原则：只挡「绝对不该进仓库」的东西（系统垃圾 / 编辑器缓存 / 密钥 /
#       依赖目录 / 构建产物 / 本机运行时状态）。
#       文档、图片、方案、参考资料一律放行，不在这里忽略。
# 项目专属规则请写在该项目自己的 .gitignore 里，例如：
#       我的项目\<项目名>\.gitignore
# 验证某条规则是否真的生效：git check-ignore -v <路径>
# ============================================================

# ---------- 操作系统垃圾文件 ----------
Thumbs.db
Desktop.ini
.DS_Store
*~

# ---------- 编辑器 / IDE ----------
.vscode/
.idea/
*.swp
*.swo
*.tmp
*.code-workspace

# ---------- 日志 ----------
*.log

# ---------- 密钥与本地私有配置 ----------
.env
.env.*
!.env.example
*.local

# ---------- 依赖目录 ----------
node_modules/
venv/
__pycache__/

# ---------- 构建产物与缓存 ----------
dist/
build/
out/
coverage/
test-results/
*.tsbuildinfo
.codex-run/
.script-test/

# ---------- AI 助手运行时状态（本机私有，不共享） ----------
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
.claude/assistant-daemon-state.json
.claude/agent-registry.json
.claude/first-run
.claude/agent-memory-local/
.claude/routines/.state/
.claude/worktrees/
.claude/checkpoints/
.claude/mailbox/
# harness hook 的运行时产物（第 2 批）：会话配额、逃生阀使用记录
.claude/state/
# harness 工作目录（HANDOFF / 日志 / 逃生阀标记）
.harness/
# harness 日志中心（第 2 段 · 要求 A）：只追加、不属于交付物
# ★ 日志**内容**不进 git；但说明文件与目录结构要随仓库走（否则克隆下来没有日志中心，自检会红）。
#   注意 gitignore 语义：被忽略目录里的文件**无法**用 ! 单独放行 —— 必须先 日志/* 再放行具体文件。
日志/*
!日志/README.md
# hook 覆盖时的备份（第 2 段 · P1-3）：备份是本机行为，不该被提交
.claude/*.bak-*

# ---------- 嵌套的独立 git 仓库 ----------
DevPilot-dev/devpilot/
